import { DirectiveeDirective } from './directivee.directive';

describe('DirectiveeDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveeDirective();
    expect(directive).toBeTruthy();
  });
});
